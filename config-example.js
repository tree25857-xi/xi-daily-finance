// ====== 曦曦的 API Token 設定檔 ======
// ⚠️ 這個檔案不會上傳到 GitHub
// ⚠️ 樹可以把這個檔案放在本地使用

const XI_CONFIG = {
  // Alpha Vantage API Token
  // 取得方式：https://www.alphavantage.co/support/#api-key
  ALPHA_VANTAGE_TOKEN: 'QQBP55WLR9ZAKT8R',
  
  // 其他 API Key 可以加在這裡
  // EXAMPLE_API_KEY: 'your-key-here'
};

// 如果需要從環境變數讀取，可以使用：
// const API_TOKEN = process.env.API_TOKEN || XI_CONFIG.ALPHA_VANTAGE_TOKEN;
